---
import type { GetStaticPaths } from "astro";
import type { Inn } from "../../../../lib/types";
import { innStaticPaths } from "../../../../data/inns";
import InnPage from "../../../../components/layout/InnPage.astro";

export const getStaticPaths = (() => innStaticPaths()) satisfies GetStaticPaths;

interface Props {
  inn: Inn;
}

const { inn } = Astro.props;
---

<InnPage inn={inn} lang="zh" />
